import { disable } from 'workbox-navigation-preload/disable.js';
import { enable } from 'workbox-navigation-preload/enable.js';
import { isSupported } from 'workbox-navigation-preload/isSupported.js';
import 'workbox-navigation-preload/_version.js';
interface NavigationPreloadState {
    enabled?: boolean;
    headerValue?: string;
}
interface NavigationPreloadManager {
    disable(): Promise<void>;
    enable(): Promise<void>;
    getState(): Promise<NavigationPreloadState>;
    setHeaderValue(value: string): Promise<void>;
}
declare global {
    interface ServiceWorkerRegistration {
        readonly navigationPreload: NavigationPreloadManager;
    }
}
/**
 * @module workbox-navigation-preload
 */
export { disable, enable, isSupported };
